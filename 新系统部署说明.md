# QHXM管理系统 - 新系统部署说明

## 📋 系统概述

QHXM管理系统已完成从传统PHP架构到现代化Laravel框架的全面升级。新系统基于以下技术栈：

- **后端框架**: Laravel 10.x (PHP 8.1+)
- **前端框架**: Bootstrap 5.3 + Bootstrap Icons
- **数据库**: MariaDB 10.6+ (兼容MySQL 5.7+)
- **服务器**: Nginx 1.18+ / Apache 2.4+
- **缓存**: Redis 6.0+ (必需)
- **队列**: Laravel Queue (Redis驱动)

## 🚀 环境要求

### 最低系统要求
- PHP 8.1 或更高版本
- MariaDB 10.6+ 或 MySQL 5.7+
- Composer 2.0+
- 内存: 2GB RAM (生产环境建议4GB+)
- 存储: 10GB+ 可用空间

### PHP扩展要求
```bash
# 必需扩展
php8.1-fpm
php8.1-mysql
php8.1-mbstring
php8.1-xml
php8.1-curl
php8.1-zip
php8.1-gd
php8.1-bcmath

# 建议扩展
php8.1-redis      # 缓存支持
php8.1-opcache    # 性能优化
php8.1-intl       # 国际化
```

## 📦 部署步骤

### 步骤1: 环境准备

```bash
# 1.1 更新系统包
sudo apt update && sudo apt upgrade -y

# 1.2 安装PHP和相关扩展（包含Redis扩展）
sudo apt install php8.1 php8.1-fpm php8.1-mysql php8.1-mbstring \
php8.1-xml php8.1-curl php8.1-zip php8.1-gd php8.1-bcmath php8.1-redis -y

# 1.3 安装Composer
curl -sS https://getcomposer.org/installer | sudo php -- \
--install-dir=/usr/local/bin --filename=composer

# 1.4 安装MariaDB
sudo apt install mariadb-server mariadb-client -y
sudo mysql_secure_installation

# 1.5 安装Nginx
sudo apt install nginx -y

# 1.6 安装Redis服务器
sudo apt install redis-server -y
sudo systemctl enable redis-server
sudo systemctl start redis-server
```

### 步骤2: 数据库配置

```sql
-- 2.1 登录MySQL
sudo mysql -u root -p

-- 2.2 创建数据库和用户
CREATE DATABASE qhxmdb CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'qhxm_user'@'localhost' IDENTIFIED BY 'YourSecurePassword123';
GRANT ALL PRIVILEGES ON qhxmdb.* TO 'qhxm_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

### 步骤3: 项目部署

```bash
# 3.1 克隆或复制项目文件
cd /var/www
sudo git clone [项目仓库地址] qhxm.com
# 或复制现有项目文件
sudo cp -r /opt/homebrew/var/www/qhxm.com /var/www/

# 3.2 设置目录权限
cd /var/www/qhxm.com/laravel-qhxm
sudo chown -R www-data:www-data .
sudo chmod -R 755 storage
sudo chmod -R 755 bootstrap/cache

# 3.3 安装依赖
composer install --no-dev --optimize-autoloader
npm install && npm run build  # 如果有前端资源

# 3.4 环境配置
cp .env.example .env
php artisan key:generate
```

### 步骤4: 环境变量配置

编辑 `/var/www/qhxm.com/laravel-qhxm/.env` 文件：

```env
APP_NAME="QHXM管理系统"
APP_ENV=production
APP_KEY=base64:uxr+RHZDZ7pa7ynLBDmrQ1+Zj7jyTiyag5mbHN01Af0=
APP_DEBUG=false
APP_URL=https://your-domain.com

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=qhxmdb
DB_USERNAME=qhxm_user
DB_PASSWORD=YourSecurePassword123

SESSION_DRIVER=redis
SESSION_LIFETIME=120

CACHE_DRIVER=redis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=12345678
REDIS_PORT=6379

QUEUE_CONNECTION=redis

MAIL_MAILER=smtp
MAIL_HOST=smtp.your-domain.com
MAIL_PORT=587
MAIL_USERNAME=mail@your-domain.com
MAIL_PASSWORD=your-email-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="noreply@your-domain.com"
MAIL_FROM_NAME="${APP_NAME}"
```

### 步骤5: Nginx配置

创建配置文件 `/etc/nginx/sites-available/qhxm.com`：

```nginx
server {
    listen 80;
    listen [::]:80;
    server_name your-domain.com www.your-domain.com;
    root /var/www/qhxm.com/laravel-qhxm/public;
    index index.php;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";
    add_header X-XSS-Protection "1; mode=block";

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.1-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }

    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # 限制上传文件大小
    client_max_body_size 100M;
}
```

启用站点配置：
```bash
sudo ln -s /etc/nginx/sites-available/qhxm.com /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

### 步骤6: 数据库迁移和初始化

```bash
cd /var/www/qhxm.com/laravel-qhxm

# 6.1 运行数据库迁移（可选 - 如果保留现有数据可跳过）
# php artisan migrate

# 6.2 创建存储链接
php artisan storage:link

# 6.3 清除缓存
php artisan config:cache
php artisan route:cache
php artisan view:cache

# 6.4 设置任务调度（Cron Job）
sudo crontab -e
# 添加以下行
* * * * * cd /var/www/qhxm.com/laravel-qhxm && php artisan schedule:run >> /dev/null 2>&1

# 6.5 设置队列工作进程（可选）
sudo nano /etc/supervisor/conf.d/laravel-worker.conf
```

Supervisor配置示例：
```ini
[program:laravel-worker]
process_name=%(program_name)s_%(process_num)02d
command=php /var/www/qhxm.com/laravel-qhxm/artisan queue:work --sleep=3 --tries=3
autostart=true
autorestart=true
stopasgroup=true
killasgroup=true
user=www-data
numprocs=2
redirect_stderr=true
stdout_logfile=/var/www/qhxm.com/laravel-qhxm/storage/logs/worker.log
stopwaitsecs=3600
```

### 步骤7: SSL证书配置（HTTPS）

```bash
# 使用Certbot获取Let's Encrypt证书
sudo apt install certbot python3-certbot-nginx -y
sudo certbot --nginx -d your-domain.com -d www.your-domain.com
```

### 步骤8: 防火墙配置

```bash
# 允许HTTP/HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 22/tcp  # SSH
sudo ufw enable
```

## 🔧 系统配置说明

### 用户角色和权限
系统内置以下用户角色：
- **管理员** (role_id=1): 完全系统访问权限
- **普通用户** (role_id=2): 基础功能访问权限
- **设备操作员** (role_id=3): 设备管理和工单操作权限

### 设备管理配置
系统支持五类设备管理：
1. **分散机** (FS) - 分散工艺设备
2. **研磨机** (YM) - 研磨工艺设备  
3. **空压机** (KY) - 空气压缩设备
4. **冰水机** (BS) - 冷却水设备
5. **废气设备** (FQ) - 废气处理设备

### 工单状态流转
工单状态定义：
- **0: 未领取** - 等待操作员领取
- **1: 已领取** - 操作员已接受任务
- **2: 进行中** - 正在生产中
- **3: 已完成** - 生产完成

### 维修订单状态
维修流程状态：
- **0: 提交维修** - 用户提交维修申请
- **1: 主管确认** - 主管确认维修需求
- **2: 机修确认** - 机修人员确认接收
- **3: 机修完成** - 机修完成维修工作
- **4: 主管确认** - 主管确认维修完成

## 🛠️ 维护指南

### 日常维护命令

```bash
# 清除缓存
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear

# 优化性能
php artisan config:cache
php artisan route:cache
php artisan view:cache
php artisan optimize

# 查看日志
tail -f storage/logs/laravel.log

# 监控队列
php artisan queue:work --queue=high,default --tries=3
```

### 备份策略

```bash
# 数据库备份脚本 /usr/local/bin/backup-qhxm.sh
#!/bin/bash
BACKUP_DIR="/backup/qhxm"
DATE=$(date +%Y%m%d_%H%M%S)
DB_NAME="qhxmdb"

# 创建备份目录
mkdir -p $BACKUP_DIR

# 备份数据库
mysqldump -u qhxm_user -p'YourSecurePassword123' $DB_NAME > \
$BACKUP_DIR/qhxmdb_$DATE.sql

# 压缩备份文件
gzip $BACKUP_DIR/qhxmdb_$DATE.sql

# 保留最近30天备份
find $BACKUP_DIR -name "*.sql.gz" -mtime +30 -delete

# 设置定时备份（每天2点）
# sudo crontab -e 添加：
# 0 2 * * * /usr/local/bin/backup-qhxm.sh
```

### 监控和告警

推荐监控项目：
1. **服务器资源**: CPU、内存、磁盘使用率
2. **服务状态**: Nginx、PHP-FPM、MySQL
3. **应用性能**: 响应时间、错误率
4. **安全监控**: 登录尝试、可疑活动

## 🔒 安全配置

### 1. 文件权限设置
```bash
# 推荐文件权限
sudo chown -R www-data:www-data /var/www/qhxm.com
sudo find /var/www/qhxm.com -type f -exec chmod 644 {} \;
sudo find /var/www/qhxm.com -type d -exec chmod 755 {} \;
sudo chmod -R 775 storage bootstrap/cache
```

### 2. PHP安全配置
```ini
; /etc/php/8.1/fpm/php.ini
expose_php = Off
display_errors = Off
log_errors = On
upload_max_filesize = 100M
post_max_size = 100M
max_execution_time = 300
```

### 3. 数据库安全
```sql
-- 定期清理过期会话
CREATE EVENT cleanup_sessions
ON SCHEDULE EVERY 1 HOUR
DO DELETE FROM sessions WHERE last_activity < UNIX_TIMESTAMP(DATE_SUB(NOW(), INTERVAL 2 DAY));
```

## 🚨 故障排除

### 常见问题及解决方案

#### 1. 502 Bad Gateway错误
```bash
# 检查PHP-FPM状态
sudo systemctl status php8.1-fpm
sudo journalctl -u php8.1-fpm

# 重启服务
sudo systemctl restart php8.1-fpm
sudo systemctl restart nginx
```

#### 2. 数据库连接失败
```bash
# 测试数据库连接
php -r "
try {
    \$db = new PDO('mysql:host=127.0.0.1;dbname=qhxmdb', 'qhxm_user', 'YourSecurePassword123');
    echo '连接成功';
} catch(Exception \$e) {
    echo '连接失败: ' . \$e->getMessage();
}
"

# 检查MySQL服务
sudo systemctl status mariadb
```

#### 3. 权限问题
```bash
# 修复文件权限
sudo chown -R www-data:www-data /var/www/qhxm.com/laravel-qhxm
sudo chmod -R 755 storage bootstrap/cache
sudo chmod -R 775 storage/logs
```

#### 4. 缓存问题
```bash
# 清除所有缓存
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear
sudo systemctl restart php8.1-fpm
```

### 日志文件位置
- 应用日志: `/var/www/qhxm.com/laravel-qhxm/storage/logs/laravel.log`
- Nginx错误日志: `/var/log/nginx/error.log`
- PHP-FPM日志: `/var/log/php8.1-fpm.log`
- MySQL日志: `/var/log/mysql/error.log`

## 📈 性能优化建议

### 1. 启用OPcache
```ini
; /etc/php/8.1/fpm/conf.d/10-opcache.ini
opcache.enable=1
opcache.memory_consumption=256
opcache.interned_strings_buffer=16
opcache.max_accelerated_files=10000
opcache.revalidate_freq=2
opcache.fast_shutdown=1
```

### 2. 配置Redis缓存
```bash
# 安装Redis
sudo apt install redis-server -y

# 配置Laravel使用Redis
# .env文件中修改：
CACHE_DRIVER=redis
QUEUE_CONNECTION=redis
```

### 3. Nginx性能优化
```nginx
# /etc/nginx/nginx.conf
worker_processes auto;
worker_connections 1024;
keepalive_timeout 65;
gzip on;
gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
```

### 4. 数据库优化
```sql
-- 添加索引优化查询
ALTER TABLE work_order ADD INDEX idx_work_state (work_state);
ALTER TABLE tb_device_repair_order ADD INDEX idx_repair_status (repair_status);
ALTER TABLE fssb ADD INDEX idx_machine_status (machine_status);

-- 定期优化表
OPTIMIZE TABLE work_order, tb_device_repair_order, fssb, ymsb, kyjsb, bsjsb, fqsb;
```

## 📞 支持与联系

### 紧急联系方式
- **系统管理员**: admin@your-domain.com
- **技术支持**: support@your-domain.com
- **值班电话**: +86-XXX-XXXX-XXXX

### 文档资源
- Laravel官方文档: https://laravel.com/docs
- Bootstrap 5.3文档: https://getbootstrap.com/docs/5.3
- MariaDB文档: https://mariadb.com/kb/en/

### 版本信息
- 当前版本: v2.0.0 (基于Laravel 10.x)
- 升级日期: 2026年4月3日
- 技术支持周期: 3年

---

## 📝 附录

### A. 部署检查清单
- [ ] 服务器环境准备完成
- [ ] 数据库创建和配置完成
- [ ] 项目文件部署完成
- [ ] 环境变量配置正确
- [ ] Nginx/Apache配置完成
- [ ] SSL证书安装完成
- [ ] 文件权限设置正确
- [ ] 缓存和队列配置完成
- [ ] 备份策略设置完成
- [ ] 监控告警配置完成

### B. 常用命令速查
```bash
# 系统服务管理
sudo systemctl status nginx
sudo systemctl restart php8.1-fpm
sudo systemctl reload nginx

# 应用管理
php artisan migrate
php artisan db:seed
php artisan storage:link

# 监控命令
htop  # 系统资源监控
mytop # MySQL监控
tail -f storage/logs/laravel.log  # 实时日志
```

### C. 紧急恢复流程
1. 检查服务器状态和日志
2. 临时切换到维护页面
3. 从备份恢复数据库（如需要）
4. 验证应用功能
5. 逐步恢复正常服务

---

**文档版本**: v1.0  
**最后更新**: 2026年4月3日  
**部署环境**: Production  
**系统状态**: ✅ 运行正常